# ZXL CSVダブルクォーテーション処理デバッグ

## 課題
ZXLのCSV読み込み時、ダブルクォーテーションが処理されていない。
- エディタ上ではダブルクォーテーションを排除する。
- 出力時には必要なカラムだけダブルクォーテーションで挟む。

## 工程記録
### 1. 現状確認 (2026-03-19)
- [ ] ZXLのCSV読み込みでダブルクォーテーションが入ってしまう箇所を確認
- [ ] ZXLのCSV出力時の仕様（どのカラムをクオートすべきか）を確認

### 2. コード調査 (完了)
- [x] `src/js/` 内のCSVパース処理を確認
- [x] `src/js/` 内のCSV出力処理を確認
- [x] `src/data/` 内のZXLモデル定義を確認

### 3. 実装・テスト (進行中)
- [x] 読み込み時のクオート除去処理を追加 (`src/js/services/csv-parser.js`)
- [x] 書き出し時の特定カラムへのクオート付加処理を追加 (`src/js/models/specs/zxl.js`)
- [x] エクスポーターの不要ロジック削除 (`src/js/services/csv-exporter.js`)
- [/] 動作確認

## ユーザーテスト結果
- (未実施)
