# ZXL CSV出力不具合デバッグログ

## 1. 課題（問題の把握）
出力機種を `ZX-L` (仕様上のIDは `zx2l`) にした場合に出力されたCSVファイルが、実機の `ZX-L` に読み込めない。

### 実機から出力された空の電話帳CSVの冒頭10行の調査：
```csv
0,1,1,0,0,0,"""""","""""","""""",16,0,"""""",16,0,"""""",16,0,"""""",16,0
```
名称・フリガナ・電話番号スロットなどの文字列型カラム（空値）が `""""""`（ダブルクォーテーション6つ）になっている。

### 実機テスト用CSVの調査：
`電話帳テスト_ZXL.csv` を確認したところ、値が入っている場合も以下のようになっていた：
```csv
0,1,1,0,0,0,"""test00000""","""TEST00000""","""1234567890""",16,0,"""""",16,0,"""""",16,0,"""""",16,0
```
- 値がある場合：`"""値"""`（ダブルクォーテーション3つで囲まれる）
- 値がない場合：`""""""`（ダブルクォーテーション6つで囲まれる）

### 原因の特定：
ZX-LのCSVパーサー/ジェネレーターは、対象のテキストカラム（`name`, `furigana`, `phone1`〜`phone4`）について、「ダブルクォーテーションで一度囲んだ値」をさらにCSVとしてエスケープした二重クォート構造（値ありなら `"""値"""`、空なら `""""""`）を期待している。
現在の本アプリのCSVエクスポート処理（`csv-exporter.js`）は、空のテキストカラムに対して `"","","",...`（通常のクォート）を出力しているため、実機側でフォーマット不正となり読み込めない。

### 追加の微修正要求（2026-05-18）:
1. **出力形式を UTF-8 に変更する**。
2. **電話番号に `-`（ハイフン）が含まれている場合はエラー対象（SEVERITY.ERROR）にする**。

---

## 2. 解決アプローチ（設計方針）
1. **SSOT・モジュール化の遵守**：
   * `csv-exporter.js` 内に機種ID `zx2l` を直接ハードコーディングすることは避け、機種仕様定義（`src/js/models/specs/zx2l.js`）に宣言的なフラグ `doubleQuoteForForceQuote` を追加する。
   * **UTF-8化**: `zx2l.js` の `encoding` プロパティを `'Shift_JIS'` から `'UTF-8'` に変更する。
   * **電話番号ハイフンエラー**: `zx2l.js` に電話番号固有の禁止文字を配列として定義する `forbiddenPhoneChars: ['-']` プロパティを導入する。
2. **エクスポート処理の修正**：
   `csv-exporter.js` の `escapeField` 関数において、`spec.doubleQuoteForForceQuote` が有効かつ `forceQuote` 対象のカラムである場合、フィールドの値をあらかじめ一度ダブルクォーテーションで囲んだ状態（`'"' + str + '"'`）にしてからCSVエスケープ処理（`str.replace(/"/g, '""')`）を適用するようにする。
3. **バリデーション処理の拡張**：
   `src/js/services/validator.js` の `validateRow` 内の電話番号検証箇所において、`spec.forbiddenPhoneChars` が定義されており、入力された電話番号にその禁止文字が含まれている場合に `SEVERITY.ERROR` を返すように実装する。

---

## 3. 作業工程と現在のステータス
- [x] 1. 一時ファイル `debug_zxl_csv_issue.md` の作成（完了）
- [x] 2. 機種仕様定義 `src/js/models/specs/zx2l.js` へのフラグ追加（完了）
- [x] 3. CSVエクスポーター `src/js/services/csv-exporter.js` の修正（完了）
- [x] 4. ローカルでの動作検証（シミュレーションテストによる出力CSVのダブルクォーテーション数確認）（完了・期待通りのクォーテーション数に修正されたことを確認）
- [x] 5. 追加の微修正要求（UTF-8化および電話番号ハイフンエラー）の実装（完了）
- [x] 6. 追加修正のローカルでの動作検証（完了・エンコーディングとバリデーション動作の両方でアサートが成功）
- [/] 7. ユーザーへのテスト依頼と結果の確認（現在テスト依頼中）

---

## 4. ユーザーテスト結果
- （修正前のテスト：実機で読み込みエラー）
- （修正後のテスト：ユーザー様に検証を依頼中）
